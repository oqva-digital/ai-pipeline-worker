# Copy this file to .env and fill in the values. Never commit .env.
# These credentials can be shared across multiple servers/instances.

# Redis connection URL (required)
REDIS_URL=redis://localhost:6379

# GitHub Personal Access Token (required)
# Get from: gh auth login, then gh auth token
# Or create at: https://github.com/settings/tokens
GITHUB_TOKEN=

# Claude OAuth Credentials (required for Claude Code CLI)
# IMPORTANT: Copy ALL three values - refresh token enables auto-renewal
# Get from macOS Keychain: security find-generic-password -s "Claude Code-credentials" -w
# Or Linux/file: cat ~/.claude/.credentials.json
# Copy accessToken, refreshToken, and expiresAt from claudeAiOauth object
CLAUDE_CODE_OAUTH_TOKEN=
CLAUDE_CODE_REFRESH_TOKEN=
CLAUDE_CODE_EXPIRES_AT=

# Google API Key (optional)
GOOGLE_API_KEY=

# v0 MCP API Key (optional - enables v0 tool inside Claude Code)
# Get from: https://v0.dev/chat/settings → API Keys
V0_API_KEY=
